export const getPeriod = (dateString: string, timeString: string, timesptamp: number) => {
  const [day, month, year] = dateString.split(".");
  const [hour, minute] = timeString.split(":");
  return 0;
}